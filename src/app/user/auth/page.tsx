'use client';

// import { getRedirectUrlByError } from '@/lib/handleError';
// import { useRouter } from 'next/navigation';
// import { useEffect } from 'react';

// TODO: 백엔드 API 완료되면 코드 작성 필요
const Auth = () => {
  // const router = useRouter();
  // const searchParams = useSearchParams();

  // const code = searchParams.get('code');
  // const error = searchParams.get('error');

  // useEffect(() => {
  //   if (error) {
  //     router.push(getRedirectUrlByError(error));
  //   }

  //   // 임시처리
  //   if (code) router.push('/');

  // }, []);

  return <section>Auth Page...</section>;
};

export default Auth;
